import areas

print(areas.triangle(2, 4))
print(areas.rectangle(3, 2))
print(areas.circle(3))

#For creating a python module separated by different files, we have to
#use a __init__.py to indicate that all the files are one modules